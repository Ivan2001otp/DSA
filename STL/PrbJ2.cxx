#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

/*
cin >> N;
    vector<ll> v(N);

    for (ll i = 0; i < N; i++)
    {
        cin >> v[i];
    }

    cin >> Q;

    while (Q--)
    {
        ll i;
        cin >> i;
        bool flag = false;
        i--;

        ll j;

        for (j = i + 1; j < N; j++)
        {
            if (v[j] > v[i])
            {
                flag = true;
                break;
            }
        }

        if (flag)
        {
            cout << j + 1 << endl;
        }
        else
        {
            cout << -1 << endl;
        }
    }

*/
int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

    ll N, Q;
    
    return 0;
}